"use strict";

//For this project a config file isn't really necessary but it's good practice and I would normally include one
let config = {
    swapiRootURL: "http://swapi.co/api"
};

module.exports = config;